@НаСервере @ДоступноСКлиента
статический метод ПрочитатьXmlСхему(СтрокаXml: Ууид): Строка
    знч СсылкаНа = НастройкиПодключенияК1с.ПолучитьСсылку(СтрокаXml).ЗагрузитьОбъект()
    // Объект чтения строки XML
    знч Чтение = новый ЧтениеXml(СсылкаНа.Схема)

    пока Чтение.Следующий()
        // Начало элемента
        если Чтение.ВидУзла == ВидУзлаXml.НачалоЭлемента
        // Начало элемента Заголовок


        ;
    ;

    // Создание объекта с результатом чтения XML
    возврат ""
;

метод СхемаПриИзменении(Источник: ПолеВвода<НастройкиПодключенияК1с.Ссылка?>,
        Событие: СобытиеПриИзменении<НастройкиПодключенияК1с.Ссылка?>)

    ПрочитатьXmlСхему(Источник.Значение.Ид)
;